package com.cyrusinnovation.computation.persistence

import org.scalatest.{Matchers, FlatSpec}

class GraphSaveTest extends FlatSpec with Matchers {

  "A library" should "be able to be saved under the root node" is (pending)
//    val library = Library("Test", List())
//    val rootNode = GraphStore.rootNode
//    versions.addNodeUnder(version)
//
//    val result = GraphStore.retrieveNode(Version, "1.1")
//    result should be(version)
//  }

}
